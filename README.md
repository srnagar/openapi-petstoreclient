# Generated Petstore client

This repository contains a sample for generating a petstore client from OpenAPI spec using OpenAPI generator maven plugin.

### Generate client

To generate client, run the following maven command:

```bash
mvn clean install
```
